name: github-release

on:
  workflow_dispatch:

jobs:
  release:
    uses: wittdennis/pipelines/.github/workflows/cog-release.yaml@d30ae383896657cd09cf53e41b71edbe06a58a6d # 2.0.17
    secrets:
      release-token: ${{ secrets.RELEASE_TOKEN }}
