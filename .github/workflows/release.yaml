name: github-release

on:
  workflow_dispatch:

jobs:
  release:
    uses: wittdennis/pipelines/.github/workflows/cog-release.yaml@af2139700a0a968d6532accf551b4c7b531f7fc3 # 2.0.19
    secrets:
      release-token: ${{ secrets.RELEASE_TOKEN }}
